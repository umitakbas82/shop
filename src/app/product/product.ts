import { Component, OnInit } from '@angular/core';
export default class Product{
    id!: number;
    name!: string;
    price!: number;
    categoryId!: number;
    description!: string;
    imageUrl!: string;
    }